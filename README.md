# CSS GRID LAYOUTS

Install live-server:

```
npm install -g live-server
```

Run project:

```
live-server
```

This command opens default browser and user can navigate to select files in src folder

Server supports hot reload
If file was edited and saved in editor - the result of change will be immediately represented in the browser's window
